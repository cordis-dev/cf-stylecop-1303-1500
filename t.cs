public class T
{
   public static Logger Metrics(
      this LoggerSink sink,
      int days = -1)
   {
      const string logFileExtension = ".yml";
   }
   
   private bool AllFieldsDone =>
      new [] { one }.All(i => !string.IsNullOrWhiteSpace(i.text));
}
